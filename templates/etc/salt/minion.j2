# {{ ansible_managed }}
{% if saltstack_minion_configuration is defined and saltstack_minion_configuration != [] %}
{%   for key, value in saltstack_minion_configuration.items()|sort %}
{%     if value and (value is not iterable or value is string) %}
{{ key }}: {{ value }}
{%     elif value is iterable and value is not string and value != [] %}
{{ key }}:
{%       for _value in value %}
  - {{ _value }}
{%       endfor %}
{%     endif %}
{%   endfor %}
{% endif %}
